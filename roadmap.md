# Roadmap

- [x] Fix real-time HLS `http: 0` playback by applying Google Storage CORS.
- [x] Extend provisioning and admin recovery to verify public access and CORS independently.
- [x] Show public-access and CORS status separately in admin diagnostics.
- [x] Deploy and repair the bucket without restarting the broadcast; verify CORS headers. Playback files were removed after the broadcast ended normally.
